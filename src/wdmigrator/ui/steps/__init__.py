"""The seven wizard steps, in order: connect, select, resolve, conflicts,
confirm, execute, results. Each module exposes `render(state)` and
`gate(state) -> list[Blocker]`; see `wdmigrator.ui.app` for how they're
driven."""
