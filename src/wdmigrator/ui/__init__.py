"""The Streamlit wizard. The only subpackage of `wdmigrator` allowed to
import `streamlit` or `pandas` — see `wdmigrator.api` for why, and
`tests/test_ui_boundaries.py` for the check that keeps it that way."""
