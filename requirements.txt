# Dependencies are declared in pyproject.toml (single source of truth).
# This file exists so `pip install -r requirements.txt` still works.
#
# Installs wdmigrator in editable mode plus the dev/test extras:
#   zeep, requests, python-dotenv, lxml, click, pytest
-e .[dev]
